fastapi>=0.115.0
uvicorn>=0.32.0
httpx>=0.28.0
python-dotenv>=1.0.0
asgiref>=3.8.0     # Required by Vercel Python runtime for ASGI apps

# ── Database ──
sqlalchemy>=2.0.0
alembic>=1.13.0
# ── Payments (Paymob via REST API) ──
requests>=2.31.0

# ── HTTP client (for async API calls) ──
aiohttp>=3.9.0

# ── Rate limiting ──
slowapi>=0.1.9

# ── Auth helpers (Clerk webhook calls) ──
